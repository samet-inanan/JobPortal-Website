@model IEnumerable<JobPortal.Models.Ulke>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="genric-btn bg-dark">Create New</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ulke1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ulke1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UlkeId" class="genric-btn primary">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UlkeId" class="genric-btn info">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UlkeId" class="genric-btn danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
